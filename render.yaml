services:
  - type: web
    name: stockAnalysis
    env: python
    region: oregon
    plan: free
    branch: mainStock
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.main:app --bind 0.0.0.0:8000
    envVars:
      - key: GROQ_API_KEY
        sync: false